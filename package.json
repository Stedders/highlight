{
  "dependencies": {
    "@tailwindcss/typography": "^0.4.0",
    "autoprefixer": "^10.2.4",
    "tailwindcss": "^2.0.3"
  },
  "scripts": {
    "test": "python -m pytest -v --cov=app --cov-report html"
  }
}
